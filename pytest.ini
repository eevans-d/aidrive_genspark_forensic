[pytest]
# Filtro enfocado en fase de hardening del dashboard.
# Excluimos subsistemas legacy o no cubiertos ahora para evitar fallos de colección.
addopts = -k "not learning_system and not agente_deposito and not integration and not stock_manager and not test_config and not provider_logic" --cov=./inventario-retail/web_dashboard/dashboard_app.py --cov-report=term-missing
python_files = test_*.py
